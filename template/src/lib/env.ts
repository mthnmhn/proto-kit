/**
 * Check if running in local dev mode.
 * IMPORTANT: Use import.meta.env.DEV directly in components for proper tree-shaking.
 * This file exists only as documentation — do not re-export the value.
 */
